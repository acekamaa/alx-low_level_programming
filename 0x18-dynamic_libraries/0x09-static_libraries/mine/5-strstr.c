#include "main.h"

/**
 * *_strstr - entry point
 * @haystack: holds first char
 * @needle: holds second char
 *
 * Return: strstr
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}

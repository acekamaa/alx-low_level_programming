#include "main.h"

/**
 * *_memset - entry point
 * @s: pointer to character
 * @b: holds second char
 * @n: unsigned int
 *
 * Return: memset
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}

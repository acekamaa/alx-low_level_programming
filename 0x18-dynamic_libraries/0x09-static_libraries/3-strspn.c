#include "main.h"

/**
 * _strspn - spans string
 * @s: pointer to first char
 * @accept: pointer to second char
 *
 * Return: strspn
 */
unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}

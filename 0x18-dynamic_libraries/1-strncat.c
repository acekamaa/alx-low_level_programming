#include "main.h"

/**
 * *_strncat - entry point
 * @dest: holds destination
 * @src: holds source
 * @n: holds the value
 *
 * Return: strncat
 */
char *_strncat(char *dest, char *src, int n)
{
	return (strncat(dest, src, n));
}

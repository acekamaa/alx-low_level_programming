/* program to output alphabets in lower case */
#include <stdio.h>
void print_alphabet(void);
{
	if (a = 'a' && a == 'z')
	{
		if (a == 'A' && a == 'Z')
		{
			tolower(a);
			putchar(a);
		}
		putchar(a);
	}

}
int main()
{
	int a;
/* prototype call */
	print_alphabet(a);
	return 0;
} 


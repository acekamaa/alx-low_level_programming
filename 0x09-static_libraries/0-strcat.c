#include "main.h"

/**
 * *_strcat - entry point
 * @dest: holds char
 * @src: holds the source
 *
 * Return: strcat(dest,src)
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}

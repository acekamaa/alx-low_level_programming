#include "main.h"

/**
 * *_strcpy - enables copy of strings
 * @dest: holds dstination value in char
 * @src: holds source value in char
 *
 * Return: strcpy
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}

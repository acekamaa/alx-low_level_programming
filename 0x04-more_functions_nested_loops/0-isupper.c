#include "main.h"
#include <stdio.h>

/* _isupper checks code */
int _isupper(int c);

/* _isupper - checks if upper */
int _isupper(int c)
{
	if (c == 'A' && c == 'Z')
	{
		printf("%c: %d\n", c);
		return (1);
		else
			if (c == 'a' && c == 'z')
			{
				printf("%c: %d\n", c);
				return (0);
			}
	}
/**
 * main - Entry point checks code
 *
 * Return:Always 1(c is uppercase) 0(otherwise)
 */
int main(void)
{
	int _isupper(c);
	return (0);
}

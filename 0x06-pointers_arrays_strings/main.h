#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

/**
 * _putchar - prints to the std out
 * @char c - holds the char value
 * Return: Always 0(success)
 */
int _putchar(char c);
char *_strcat(char *dest, char *src);

#endif

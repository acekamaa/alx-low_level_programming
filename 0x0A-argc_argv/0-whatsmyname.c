#include "main.h"

/**
 * main - prints name
 * @argc: holds int
 * @argv: holds char
 *
 * Return: Always 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}

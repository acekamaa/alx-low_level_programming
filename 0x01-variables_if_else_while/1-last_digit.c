`#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>
/* betty style doc for function main goes there */
Betty()
int main(void)
{
	int n , digit;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */

	printf("Last digit of %n is: %n" , digit);
	printf("Last digit of", n , "is ");
	If (last digit of n > 5) {
	printf("and is greater than 5");
	else
	If (last digit of n == 0) {
	printf("and is 0");
	else
	If (last digit of n > 0 && last digit of n < 6) {
	printf("and is less than 6 and not 0");
	}
	}
	}
	return (0);
}

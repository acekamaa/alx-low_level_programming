#include<stdio.h>

/**
 * main - Entry point, prints single numbers of base 10 from 0
 *
 * Return: Always 0(Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	return (0);
}
